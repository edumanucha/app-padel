# Copiar este archivo como ".env" (sin ".example") y completar con tus valores reales.
# El archivo ".env" real NUNCA se sube a git (ver .gitignore).

PORT=3001

# Supabase (Project Settings -> API en supabase.com)
SUPABASE_URL=
SUPABASE_ANON_KEY=

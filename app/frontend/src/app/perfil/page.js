import VerPerfilForm from "@/components/VerPerfilForm";

// Ruta: /perfil (US-1.3)
export default function PerfilPage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center gap-6 p-8">
      <VerPerfilForm />
    </main>
  );
}

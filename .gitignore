# Dependencias y builds
node_modules/
.next/
out/
build/
coverage/

# Variables de entorno con claves reales -- NUNCA se suben
.env
.env.*
!.env.example

# Vercel (vínculo local del proyecto)
.vercel/

# Configuración local de Claude
.claude/settings.local.json

# Varios
.DS_Store
Thumbs.db
~$*
*.log

# Notas locales con secretos -- nunca al repo
docs/*.txt

# Credenciales y claves (por si alguna vez se descarga una a la carpeta)
*.pem
*.key
*.p12
*.pfx
*credential*
*credencial*
*secret*
*secreto*
client_secret*.json
service-account*.json
